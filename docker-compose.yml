services:
  dev:
    build:
      context: .
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
        USERNAME: dev
    working_dir: /app
    volumes:
      - .:/app
    command: tail -f /dev/null
